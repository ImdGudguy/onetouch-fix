services:
  web:
    build: .
    container_name: intellifix-web
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - HOSTNAME=0.0.0.0
    volumes:
      - intellifix-data:/app/data
    restart: unless-stopped

volumes:
  intellifix-data:
